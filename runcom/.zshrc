

export PATH=$PATH:~/Desktop/Workspace/flutter/bin

# The next line updates PATH for the Google Cloud SDK.
if [ -f '/Users/nandupokhrel/google-cloud-sdk/path.zsh.inc' ]; then . '/Users/nandupokhrel/google-cloud-sdk/path.zsh.inc'; fi

# The next line enables shell command completion for gcloud.
if [ -f '/Users/nandupokhrel/google-cloud-sdk/completion.zsh.inc' ]; then . '/Users/nandupokhrel/google-cloud-sdk/completion.zsh.inc'; fi
